## 1. Initial API behavior contract baseline

Section 1 records the completed baseline needed for the first context-scoped auth behavior slice. The canonical concept/capability TypeSpec refactor, schema deduplication, route DTO cleanup, and final codegen gate are executed by sections 8 and 11.

- [x] 1.1 `packages/typespec/main.tsp` と concept-based TypeSpec model modules に Product/Admin の `credentialMode`、Cookie/Bearer response、`authContextId`、context refresh request/response、failure response、clear-cookie command list、context index update hints を追加・更新する。完了条件: Product/Admin browser Cookie mode と external Bearer mode の DTO が TypeSpec 上で分離し、`[API-CONTRACT-BE-S011]`、`[API-CONTRACT-BE-S012]` の contract tests がある。
- [x] 1.2 `packages/typespec/src/routes/v1/product/**` と `packages/typespec/src/routes/v1/admin/**` に Product/Admin それぞれの `POST /api/v1/auth/contexts/{authContextId}/refresh`、login/setup/register/logout response shape、BearerAuth declaration を追加・更新する。完了条件: Product/Admin とも `/api/v1/*` のままで `/api/admin/*` がない。
- [x] 1.3 Product/Admin surface separation lint と codegen boundary を更新する。対象: `packages/typespec/.spectral.yaml`、`packages/typespec/spectral/*.js`、`packages/typespec/scripts/check-surface-boundaries.mjs`。完了条件: `[API-CONTRACT-BE-S001]`、`[API-CONTRACT-BE-S002]`、`[API-CONTRACT-BE-S003]`、`[API-CONTRACT-BE-S010]`、`[API-CONTRACT-BE-S006]`、`[API-CONTRACT-BE-S007]`、`[API-CONTRACT-BE-S008]`、`[API-CONTRACT-BE-S009]` を検出できる contract/codegen tests がある。
- [x] 1.4 `pnpm gen` を実行する。完了条件: Product OpenAPI / SDK / Go bindings と Admin OpenAPI / SDK / Go bindings が TypeSpec から再生成され、生成物を手編集していない。
- [x] 1.5 `pnpm check:codegen` を実行する。完了条件: Product/Admin generated artifacts の drift と surface contamination がない。

## 2. Shared backend auth primitives

- [x] 2.1 `packages/backend/internal/application/shared/authprimitive` などの中立 package を追加・更新し、Cookie path construction、Cookie clear command、TTL validation、opaque token hash、failure normalization を実装する。完了条件: Product/Admin domain enum、RBAC、status 判定、operator active 判定を含まない。
- [x] 2.2 shared primitive の unit/import-boundary tests を追加する。test title に `[AUTH-BE-S064]`、`[AUTH-BE-S065]`、`[AUTH-BE-S068]`、`[AUTH-BE-S082]` を含める。完了条件: Cookie TTL 上限、TTL validation、中立 primitive 境界、protected request での accessToken TTL 非延長が repository script 経由で検証される。
- [x] 2.3 Concept-based application / route adapter import-boundary tests を追加・更新する。test title に `[AUTH-BE-S067]`、`[AUTH-BE-S069]`、`[AUTH-BE-S070]`、`[AUTH-BE-S071]`、`[AUTH-BE-S072]` を含める。完了条件: account/operator eligibility ownership、HTTP adapter の generated artifact 境界、explicit account/operator subject payload conversion、concept application package boundary が検証される。

## 3. Product backend 実装

- [x] 3.1 Product session issuance を更新し、Cookie mode で body accessToken/authContextId/metadata と path-scoped refresh Cookie、Bearer mode で body accessToken/refreshToken を返す。対象: `packages/backend/internal/application/auth_service.go`、`token_service.go`、Product DTO。完了条件: `[AUTH-BE-S060]`、`[AUTH-BE-S063]` の tests が通る。
- [x] 3.2 Product context refresh を `POST /api/v1/auth/contexts/{authContextId}/refresh` に実装し、Cookie mode と Bearer mode の exactly-one refresh credential、Authorization header rejection、path ownership validation、atomic rotation、Cookie Path 非認可境界、同時 refresh race handling を実装する。完了条件: `[AUTH-BE-S062]`、`[AUTH-BE-S066]`、`[AUTH-BE-S083]`、`[AUTH-BE-S086]`、`[AUTH-BE-S087]`、`[AUTH-BE-S080]`、`[AUTH-BE-S090]`、`[AUTH-BE-S091]`、`[AUTH-BE-S044]`、`[AUTH-BE-S045]` の endpoint/application tests がある。
- [x] 3.3 Product protected route middleware を Bearer-only に更新し、Cookie credential、refresh Cookie、`X-Auth-Context-Id`、CSRF を認可材料にしない。完了条件: `[AUTH-BE-S084]`、`[AUTH-BE-S085]`、`[AUTH-BE-S009]`、`[AUTH-BE-S046]` の tests がある。
- [x] 3.4 Product logout / revoke / suspend / restore flow を accessToken claims と refresh token family に接続し、対象 refresh Cookie path の clear command を返す。完了条件: `[AUTH-BE-S042]`、`[AUTH-BE-S092]`、`[AUTH-BE-S054]`、`[AUTH-BE-S055]`、`[AUTH-BE-S058]`、`[AUTH-BE-S056]`、`[AUTH-BE-S057]`、`[AUTH-BE-S059]` の tests がある。
- [x] 3.5 Product auth state store unavailable、allowed Origin、Fetch Metadata、CORS credential policy、no-store / security header handling を更新する。完了条件: `[AUTH-BE-S010]`、`[AUTH-BE-S088]`、`[AUTH-BE-S089]` を含む fail-close endpoint tests がある。
- [x] 3.6 Product backend tests を `packages/backend/internal/adapter/http/product/*_test.go`、`packages/backend/internal/application/*_test.go` に追加・更新し、test title に 3.1〜3.5 の Scenario ID を角括弧で入れる。

## 4. Admin backend 実装

Dependency note: 4.1〜4.7 are not independently acceptable while `packages/backend/internal/application/admin/**` or `packages/backend/internal/application/product/**` still own lifecycle/auth/account/audit/operator/authorization logic. Before marking any 4.x Admin backend task complete, route the Admin behavior through the concept owners from section 10 and keep Product/Admin names only in hosted service boundaries: HTTP adapters, generated bindings, binary/runtime composition, and route artifacts.

- [x] 4.1 Admin login/setup/operator-setup issuance を更新し、Cookie mode で body accessToken/authContextId/metadata と path-scoped refresh Cookie、Bearer mode で body accessToken/refreshToken を返す。完了条件: `[ADMIN-AUTH-BE-S074]`、`[ADMIN-AUTH-BE-S079]`、`[ADMIN-AUTH-BE-S064]`、`[ADMIN-AUTH-BE-S065]` の tests がある。
- [x] 4.2 Admin context refresh を Product と同じ relative path で Admin service に実装し、Cookie/body exactly-one、Authorization header rejection、path ownership validation、atomic rotation、Bearer refresh success、reuse/theft family revocation を実装する。完了条件: `[ADMIN-AUTH-BE-S081]`、`[ADMIN-AUTH-BE-S082]`、`[ADMIN-AUTH-BE-S083]`、`[ADMIN-AUTH-BE-S084]`、`[ADMIN-AUTH-BE-S085]` の tests がある。
- [x] 4.3 Admin protected route middleware を Bearer-only に更新し、Product bearer、Product Cookie、Admin refresh Cookie、legacy access Cookie、`X-Auth-Context-Id`、CSRF を認可材料にせず、通常の protected Admin request で operator accessToken TTL を延長しない。完了条件: `[ADMIN-AUTH-BE-S057]`、`[ADMIN-AUTH-BE-S058]`、`[ADMIN-AUTH-BE-S080]`、`[ADMIN-AUTH-BE-S060]`、`[ADMIN-AUTH-BE-S078]` の tests がある。
- [x] 4.4 Admin Cookie-setting / pre-auth flows に allowed Origin、Fetch Metadata、CORS、no-store、security headers を適用する。完了条件: `[ADMIN-AUTH-BE-S059]`、`[ADMIN-AUTH-BE-S061]` の endpoint tests があり、section 10 の owner-package gate 後も Admin surface package が true owner として残っていない。
- [x] 4.5 Admin domain eligibility と Valkey namespace tests を維持・更新する。test title に `[ADMIN-AUTH-BE-S056]` を含め、既存 canonical regression として `[ADMIN-AUTH-BE-S062]`、`[ADMIN-AUTH-BE-S063]` も継続検証する。
- [x] 4.6 Admin Console backend surface / RBAC / logout を Bearer operator accessToken + server-side session record 前提へ更新する。完了条件: `[ADMIN-CONSOLE-BE-S056]`、`[ADMIN-CONSOLE-BE-S057]`、`[ADMIN-CONSOLE-BE-S058]`、`[ADMIN-CONSOLE-BE-S094]`、`[ADMIN-CONSOLE-BE-S068]`、`[ADMIN-CONSOLE-BE-S069]`、`[ADMIN-CONSOLE-BE-S095]`、`[ADMIN-AUTH-BE-S086]` の tests がある。
- [x] 4.7 Admin backend tests を `packages/backend/internal/adapter/http/admin/*_test.go`、concept-based application package tests、boundary tests に追加・更新し、4.1〜4.6 の Scenario ID を test title に入れる。

## 5. Product frontend 実装

- [x] 5.1 `packages/frontend/domain/src/auth/types.ts` と session state を、session item = `authContextId` + identity/session metadata + short-lived `accessToken` の memory-only model に更新する。完了条件: refreshToken / Cookie value が型にも state にも存在しない。
- [x] 5.2 Product API request helpers を active accessToken の `Authorization` header に統一し、protected API で `X-Auth-Context-Id` と CSRF header を生成しないようにする。完了条件: `[AUTH-FE-S057]` の unit/component test がある。
- [x] 5.3 Product context refresh URL builder、refresh-once retry、in-flight aggregation、target-session-only failure handling を実装する。完了条件: `[AUTH-FE-S045]`、`[AUTH-FE-S047]`、`[AUTH-FE-S055]` の tests がある。
- [x] 5.4 Product context index/bootstrap を origin-local `localStorage` で実装し、schema/version/namespace/expiry/cleanup、token/secret 非保存、tamper fail-close、multi-tab propagation を検証する。完了条件: `[AUTH-FE-S056]`、`[AUTH-FE-S058]`、`[AUTH-FE-S059]` の unit/e2e tests がある。
- [x] 5.5 Product AccountSwitcher と login/recovery/register/logout flows を multi-session item model に更新する。完了条件: `[AUTH-FE-S048]`、`[AUTH-FE-S049]`、`[AUTH-FE-S050]`、`[AUTH-FE-S060]`、`[AUTH-FE-S006]`、`[AUTH-FE-S007]`、`[AUTH-FE-S008]` の tests がある。
- [x] 5.6 Product secret leakage tests を追加・更新する。test title に `[AUTH-FE-S046]` を含め、memory/localStorage/sessionStorage/IndexedDB/URL/telemetry/console に refreshToken と Cookie value がないことを確認する。

## 6. Admin frontend 実装

- [x] 6.1 `packages/admin/api/src/client.ts` を Admin Console wrapper と Admin automation Bearer wrapper に分離する。完了条件: Console wrapper は active operator accessToken の Authorization を使い、refreshToken/Cookie value を受け取らない。
- [x] 6.2 `packages/admin/domain/src/auth.ts` と `useAdminSession.svelte.ts` を operator session item = `authContextId` + operator/session metadata + short-lived operator accessToken の memory-only model に更新する。完了条件: `[ADMIN-AUTH-FE-S033]` の tests がある。
- [x] 6.3 Admin current/protected route verification を active operator accessToken の Authorization header に統一し、Product SDK / Product Cookie / `X-Auth-Context-Id` / CSRF を使わない。完了条件: `[ADMIN-AUTH-FE-S027]`、`[ADMIN-AUTH-FE-S028]`、`[ADMIN-AUTH-FE-S030]`、`[ADMIN-AUTH-FE-S031]`、`[ADMIN-AUTH-FE-S034]` の tests がある。
- [x] 6.4 Admin context refresh URL builder、refresh-once retry、session-expiry cleanup を実装する。完了条件: `[ADMIN-AUTH-FE-S035]`、`[ADMIN-AUTH-FE-S036]`、`[ADMIN-AUTH-FE-S037]` の tests がある。
- [x] 6.5 Admin context index/bootstrap を origin-local `localStorage` で実装し、schema/version/namespace/expiry/cleanup、token/secret 非保存、tamper fail-close、multi-tab propagation を検証する。完了条件: `[ADMIN-AUTH-FE-S041]`、`[ADMIN-AUTH-FE-S042]`、`[ADMIN-AUTH-FE-S043]` の unit/e2e tests がある。
- [x] 6.6 Admin setup/operator-setup login completion を updated session model に接続し、setup token / refreshToken / Cookie value を保存しない。完了条件: `[ADMIN-AUTH-FE-S038]`、`[ADMIN-AUTH-FE-S039]`、`[ADMIN-AUTH-FE-S040]`、`[ADMIN-AUTH-FE-S029]` の tests がある。

## 8. Canonical TypeSpec concept organization / schema deduplication gate

- [x] 8.1 `packages/typespec/main.tsp` の common model imports と `packages/typespec/src/models/**` の auth / admin catch-all model を棚卸しし、`models/admin.tsp` が common model import として account/auth concept を抱え込まないように、認証 primitive、WebAuthn、session、refresh、logout、recovery、account read/create model、operator profile、operator setup、authorization の concept modules へ分割する。完了条件: 各 concept の owner file が一意に決まり、重複定義 inventory が削除済みで、`[API-CONTRACT-BE-S013]`、`[API-CONTRACT-BE-S017]`、`[API-CONTRACT-BE-S018]` を含む contract/source-structure tests がある。
- [x] 8.2 Product/Admin の Cookie mode / Bearer mode response、context refresh request/response、failure response、clear-cookie command、context index hint を単一 source definition から参照する形に整理し、`packages/typespec/src/models/auth.tsp` と `packages/typespec/src/models/admin.tsp` の session response duplication を shared auth envelope concept へ統合する。完了条件: `accessToken`、`refreshToken`、`authContextId`、`sessionId`、`expiresAt`、`contextIndexUpdateHints`、`clearCookieCommands`、`credentialMode` の field name が両 service artifact で一致し、Admin artifact が `operatorAccessToken` ではなく `accessToken` を公開し、`[API-CONTRACT-BE-S015]`、`[API-CONTRACT-BE-S019]` を含む generated schema tests がある。
- [x] 8.3 Account / operator 差分を explicit service-specific subject payload と metadata model で表し、generated consumer の可読性を優先して Product response は `account` field、Admin response は `operator` field を使う。TypeSpec templates/generics が generated SDK/Go usage を読みにくくする場合は shared envelope を include/spread した explicit composed response model を採用する。完了条件: `principal` wrapper を必須にせず、`AuthContextIdentityKind`、`identityKind`、`principal.kind` を Product/Admin context discriminator として要求する contract field から削除し、Admin auth contract の token field が `accessToken` になり、context index entry が service boundary で解釈でき、`[API-CONTRACT-BE-S016]`、`[API-CONTRACT-BE-S019]` を含む schema/import tests がある。
- [x] 8.4 TypeSpec route source を `packages/typespec/src/routes/v1/product/**` と `packages/typespec/src/routes/v1/admin/**` に移し、route DTO は duplicated surface DTO ではなく common auth contract concepts を参照する。完了条件: `packages/typespec/main.tsp`、namespace files、service declarations、codegen scripts が新 route tree を参照し、`[API-CONTRACT-BE-S014]`、`[API-CONTRACT-BE-S019]` を含む route-source tests がある。
- [x] 8.5 TypeSpec surface boundary checks を concept module / route-only split に合わせて更新する。対象: `packages/typespec/scripts/check-surface-boundaries.mjs`、`packages/typespec/spectral/*.js`、関連 package scripts。完了条件: `[API-CONTRACT-BE-S001]`、`[API-CONTRACT-BE-S002]`、`[API-CONTRACT-BE-S006]`、`[API-CONTRACT-BE-S007]`、`[API-CONTRACT-BE-S008]`、`[API-CONTRACT-BE-S009]`、`[API-CONTRACT-BE-S010]`、`[API-CONTRACT-BE-S013]`〜`[API-CONTRACT-BE-S019]` の regression tests がある。
- [x] 8.6 `pnpm gen` を実行して Product/Admin OpenAPI、SDK、Go bindings を再生成する。完了条件: generated artifacts は手編集なしで TypeSpec source と同期している。
- [x] 8.7 `pnpm check:codegen` を実行する。完了条件: generated drift、cross-service operation/schema/export contamination、`/api/admin/*` contamination がない。

## 9. Generated consumer updates

- [x] 9.1 Product frontend domain / API wrapper の generated type imports を unified field name と explicit `account` subject field に合わせる。完了条件: Product login / refresh / logout tests の `[AUTH-FE-S045]`、`[AUTH-FE-S046]`、`[AUTH-FE-S048]`、`[AUTH-FE-S050]`、`[AUTH-FE-S056]`、`[AUTH-FE-S057]`、`[AUTH-FE-S058]`、`[AUTH-FE-S059]`、`[AUTH-FE-S060]` が updated generated types で通る。
- [x] 9.2 Admin API wrapper / domain / app の generated type imports を `accessToken` と explicit `operator` subject field に合わせる。完了条件: surface-prefixed token field 前提の handwritten code がなくなり、`[ADMIN-AUTH-FE-S027]`、`[ADMIN-AUTH-FE-S028]`、`[ADMIN-AUTH-FE-S033]`〜`[ADMIN-AUTH-FE-S043]` の tests が updated generated types で通る。
- [x] 9.3 Backend HTTP adapters の Product/Admin generated binding usage を new route tree と unified DTO に合わせる。完了条件: Product adapter は Product bindings、Admin adapter は Admin bindingsだけを参照し、`[API-CONTRACT-BE-S007]`、`[API-CONTRACT-BE-S008]`、`[ADMIN-CONSOLE-BE-S056]`、`[ADMIN-CONSOLE-BE-S057]` の tests が通る。

## 10. Backend concept organization / use-case deduplication

Section dependency: this section is the owner-package gate for the remaining Admin backend work. 10.4、10.5、10.7〜10.10 must be completed before 4.1〜4.7 can be accepted as done, because Admin issuance/refresh/protected-route/RBAC behavior cannot rely on `internal/application/admin/**` or `internal/application/product/**` as true application/domain owner packages.

Mechanical completion gate: 10.1、10.2、10.4、10.5、10.7〜10.10 を完了扱いにする前に、実装担当と reviewer は path evidence 付きで次を確認しなければならない。`packages/backend/internal/application/admin/**` と `packages/backend/internal/application/product/**` に auth lifecycle、refresh family、session validation、logout/revoke、token primitive、eligibility、RBAC、account/operator/audit の true owner が残っていないこと。`packages/backend/internal/application/auth_contracts.go` と `packages/backend/internal/application/token_service.go` の production caller が canonical concept package へ移っていること。`packages/backend/internal/application/auth/**` が helper/type だけではなく canonical lifecycle owner を持つこと。Scenario ID tests は文字列存在ではなく、旧 owner 不在、production caller 移行、artifact/binary/HTTP route boundary、concept-domain ownership を検証すること。これらの negative evidence と `unit/backend/reviewer` の Approve が揃うまで checkbox を付けてはならない。

- [x] 10.1 `packages/backend/internal/domain/**` と `packages/backend/internal/application/**` の Product/Admin surface-as-domain duplication を棚卸しする。対象には `packages/backend/internal/application/product/auth/service.go`、`packages/backend/internal/application/admin/auth/service.go`、`packages/backend/internal/application/auth_contracts.go`、`packages/backend/internal/application/token_service.go`、`packages/backend/internal/domain/account_auth_session.go`、`packages/backend/internal/domain/operator_auth_session.go` を含める。完了条件: auth context、refresh family、session validation、logout/revoke、token primitive、WebAuthn、account eligibility、operator eligibility、authorization、audit の owner package と duplicate removal plan が実装差分に反映される。
- [x] 10.2 Concept-based application packages を作り、auth context、refresh family、session validation、logout/revoke、service-specific subject payload input、Cookie command、token primitive を整理し、Product/Admin surface package use case と legacy/root auth DTO / token service の callers を canonical auth lifecycle へ移す。完了条件: `[AUTH-BE-S093]`、`[AUTH-BE-S095]`、`[AUTH-BE-S097]`、`[AUTH-BE-S100]` を含む application/import-boundary tests がある。
- [x] 10.3 Account eligibility と operator eligibility を concept package / domain object へ配置し、neutral session/token primitive と shared refresh/session use case から explicit account/operator subject payload 経由で呼び出す。完了条件: `[AUTH-BE-S069]`、`[AUTH-BE-S070]`、`[AUTH-BE-S098]`、`[ADMIN-AUTH-BE-S057]`、`[ADMIN-AUTH-BE-S058]` の tests が通る。
- [x] 10.4 Product/Admin HTTP route adapters を service artifact boundary として維持し、generated DTO から account subject payload / operator subject payload へ変換する。完了条件: `[AUTH-BE-S094]`、`[AUTH-BE-S060]`、`[ADMIN-AUTH-BE-S074]`、`[ADMIN-AUTH-BE-S079]`、`[ADMIN-AUTH-BE-S081]`、`[ADMIN-AUTH-BE-S084]` の endpoint/application tests がある。
- [x] 10.5 Backend custom guardrails と import-boundary tests を concept organization に合わせて更新し、`packages/backend/internal/application/product_admin_auth_boundary_test.go` の Product/Admin auth domain split 固定 assertions を artifact/binary/HTTP route boundary tests と concept-domain boundary tests に置き換える。対象: `packages/backend/tools/analyzers/cmd/guardrails/main.go`、backend boundary tests。完了条件: `[AUTH-BE-S096]`、`[AUTH-BE-S071]`、`[AUTH-BE-S072]`、`[AUTH-BE-S097]`、`[AUTH-BE-S098]`、`[API-CONTRACT-BE-S007]`、`[API-CONTRACT-BE-S008]` の tests が repository script 経由で通る。
- [x] 10.6 Refresh token family / Cookie command / TTL validation / failure normalization を Product/Admin の shared concept implementation と `packages/backend/internal/application/shared/authprimitive` に寄せる。完了条件: `[AUTH-BE-S064]`、`[AUTH-BE-S065]`、`[AUTH-BE-S068]`、`[AUTH-BE-S080]`、`[AUTH-BE-S086]`、`[AUTH-BE-S087]`、`[AUTH-BE-S090]`、`[AUTH-BE-S091]`、`[ADMIN-AUTH-BE-S082]`、`[ADMIN-AUTH-BE-S083]`、`[ADMIN-AUTH-BE-S085]` の tests がある。
- [x] 10.7 Admin Console RBAC / account management backend を concept-based operator/session validation input に接続し、`account_creation_service.go`、`account_search_service.go`、`audit_service.go` の generic account/audit concepts を accounts、audit、authorization capability naming に整理する。完了条件: `[ADMIN-CONSOLE-BE-S058]`、`[ADMIN-CONSOLE-BE-S094]`、`[ADMIN-CONSOLE-BE-S068]`、`[ADMIN-CONSOLE-BE-S069]`、`[ADMIN-CONSOLE-BE-S095]`、`[ADMIN-CONSOLE-BE-S096]`、`[ADMIN-AUTH-BE-S086]` の tests がある。
- [x] 10.8 Postgres repositories を schema / aggregate / capability で整理し、`packages/backend/internal/adapter/postgres/admin/account_repository.go` が public accounts aggregate と admin audit concept を Admin surface naming だけで束ねる構成を accounts repository と audit repository へ分離する。完了条件: `[ADMIN-CONSOLE-BE-S097]` を含む repository/import-boundary tests がある。
- [x] 10.9 Product/Admin HTTP auth adapters の duplicated security/header/context helpers を `packages/backend/internal/adapter/http/shared/**` などの shared helper concept へ抽出し、route registration、generated binding import、strict handler wiring は Product/Admin adapter に残す。完了条件: `[AUTH-BE-S099]`、`[AUTH-BE-S071]`、`[AUTH-BE-S072]` の tests がある。
- [x] 10.10 Admin auth runtime config naming を operator/auth concept に揃え、`packages/backend/internal/app/admin_runtime.go` の Product-ish config field/comment/error wording を Admin operator session semantics に更新する。完了条件: `[ADMIN-AUTH-BE-S087]` を含む runtime config tests がある。

## 11. Full refactor verification gates

- [x] 11.1 TypeSpec refactor後に `pnpm format:check` と `pnpm check` を実行する。完了条件: TypeSpec format/compile と repository typecheck が成功する。
- [x] 11.2 Contract / generated refactor後に `pnpm gen` と `pnpm check:codegen` を再実行する。完了条件: generated artifacts の drift がなく、Product/Admin service artifact contamination がない。
- [x] 11.3 Backend concept organization後に `pnpm lint` と `pnpm test:server` を実行する。完了条件: 10.4、10.5、10.7〜10.10 完了後に backend guardrails、import-boundary tests、Product/Admin auth/backend Scenario ID tests が成功する。
- [x] 11.4 Frontend generated consumer更新後に `pnpm test:client` と `pnpm test:admin` を実行する。完了条件: 6.1、6.3、6.5、9.2 完了後に Product Web と Admin Console の Scenario ID tests が成功する。
- [x] 11.5 横断 refactor 完了後に `pnpm test:e2e` と `pnpm build` を実行する。完了条件: Product Web / Admin Console の login、context refresh、protected request、logout smoke と release build が成功する。
- [x] 11.6 OpenSpec artifacts を最終差分に合わせ、`openspec validate "unified-context-scoped-auth-refresh" --type change --strict --no-interactive` を実行する。完了条件: strict validation が成功する。
