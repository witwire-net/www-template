## 1. API 契約とコード生成

- [ ] 1.1 `packages/typespec/src/models/auth.tsp` と `packages/typespec/src/models/admin.tsp` に Product/Admin credential mode、Product Cookie session response、Product Bearer session response、Admin Cookie session response、Admin Bearer session response、`authContextId`、CSRF token、mode 別 refresh DTO を追加・更新する。完了条件: Product Web と Admin Console の response body が browser-readable accessToken に依存せず、Product external client と Admin API / automation client の bearer flow だけが body token を表現できる。
- [ ] 1.2 `packages/typespec/src/routes/v1/auth.tsp`、`packages/typespec/src/routes/v1/account_settings.tsp`、`packages/typespec/src/routes/admin-v1/auth.tsp`、`packages/typespec/src/routes/admin-v1/accounts.tsp` を更新し、Product passkey finish / recovery register / refresh / logout / passkey management / account settings と Admin login / setup / operator-setup / current / refresh / logout / passkey management / account mutation の契約で request/response shape、Cookie auth security、Bearer auth security、`X-Auth-Context-Id` header、`X-CSRF-Token` header を公開する。完了条件: 生成される route signature が Product/Admin の Cookie/Bearer credential transport と auth context selector を分離して表現できる。
- [ ] 1.3 `pnpm gen` を実行する。完了条件: Product artifacts (`packages/typespec/openapi/openapi.json`、`packages/backend/internal/generated/openapi/openapi.gen.go`、`packages/frontend/api/src/generated/client.ts`) と Admin artifacts (`packages/typespec/openapi/admin.openapi.json`、`packages/backend/internal/generated/adminopenapi/openapi.gen.go`、`packages/admin/api/src/generated/client.ts`) が同じ TypeSpec source から再生成されている。
- [ ] 1.4 `pnpm check:codegen` を実行する。完了条件: Product/Admin 生成 artifact に drift がなく、Product artifact に Admin operation/tag/export が混入せず、Admin artifact に Product operation/tag/export が混入していない。
- [ ] 1.5 Product/Admin API path policy を確認する。完了条件: Product と Admin はそれぞれの origin / Go binary / TypeSpec service / OpenAPI artifact / SDK package / Go bindings で分離され、どの contract / generated artifact / frontend/admin source にも `/api/admin/*` が存在しない。
- [ ] 1.6 `packages/typespec/.spectral.yaml`、`packages/typespec/spectral/app-security.js`、`packages/typespec/spectral/bearer-scheme.js`、`packages/typespec/spectral/path-policy.js`、`packages/typespec/scripts/check-surface-boundaries.mjs` を監査・更新する。完了条件: Product/Admin bearer routes は BearerAuth と `X-Auth-Context-Id` を維持し、Product/Admin Cookie credential routes は Cookie auth security、`X-Auth-Context-Id`、CSRF header を許可し、Product/Admin artifact contamination と `/api/admin/*` は検出される。

## 2. Backend Session と Credential Model

- [ ] 2.1 `packages/backend/internal/application/auth_contracts.go` を更新し、mode 別 session result、`authContextId` output、CSRF token output、Bearer refresh token output、session metadata の auth context selector と CSRF hash を表現する。完了条件: application DTO がすべての Product session response に `AccessToken` を強制しない。
- [ ] 2.2 `packages/backend/internal/application/token_service.go` を更新し、Cookie mode では access credential container、refresh credential、authContextId、CSRF token を発行し、Bearer mode では response body の accessToken / refreshToken と authContextId を発行する。完了条件: issue/refresh 呼び出し元がどちらの mode も明示的に要求でき、access credential TTL は発行時に固定され通常の protected request では延長されない。
- [ ] 2.3 `packages/backend/internal/application/auth_service.go` を更新し、passkey finish、recovery register、refresh、logout が `credentialMode`、auth context selector、session credential data を正しく伝搬する。完了条件: handler が response body field から Cookie/Bearer を推測する必要がない。
- [ ] 2.4 `packages/backend/internal/adapter/valkey/session_store.go` を更新し、session metadata の auth context selector と CSRF hash を保存・読み込みする。完了条件: selector または CSRF hash を持たない Cookie session は protected route / mutation で fail-close する。
- [ ] 2.5 `packages/backend/internal/application/token_service_test.go`、`packages/backend/internal/application/auth_service_test.go`、`packages/backend/internal/application/session_service_test.go` に `[AUTH-BE-S044]`、`[AUTH-BE-S045]`、`[AUTH-BE-S046]`、`[AUTH-BE-S060]`、`[AUTH-BE-S062]`、`[AUTH-BE-S063]`、`[AUTH-BE-S078]`、`[AUTH-BE-S079]`、`[AUTH-BE-S080]`、`[AUTH-BE-S081]` の backend application test を追加または更新する。完了条件: TokenService/AuthService の issue/refresh/expiry/theft test が mode 別 credential、authContextId、CSRF hash persistence、refresh endpoint の access Cookie 無視、Cookie refresh の事前 `X-Auth-Context-Id` / CSRF 非要求、Bearer refresh の Authorization 拒否、access TTL 非延長を網羅する。
- [ ] 2.6 `packages/backend/internal/application/admin/auth/types.go`、`packages/backend/internal/application/admin/auth/service.go`、`packages/backend/internal/application/admin/auth/payload.go` を更新し、Admin operator session result を Cookie/Bearer mode 別に表現し、Cookie mode は HttpOnly credential Cookie / refresh Cookie / authContextId / CSRF token output と session metadata の CSRF hash を返し、Bearer mode は operator accessToken / refreshToken / authContextId を返す。完了条件: Admin Cookie application DTO が Bearer token output を強制しない。
- [ ] 2.7 `packages/backend/internal/application/admin/auth/service.go`、`packages/backend/internal/application/admin/auth/service_test.go`、`packages/backend/internal/application/admin/auth/authorization.go`、`packages/backend/internal/application/admin/auth/authorization_test.go` を更新する。完了条件: login/setup/operator-setup/refresh/logout/current/mutation validation は credentialMode と `X-Auth-Context-Id` を扱い、Cookie mode は Admin credential Cookie と refresh Cookie を発行・rotation/revoke して body には authContextId、operator/session metadata、CSRF token だけを返し、Bearer mode は operator accessToken / refreshToken と authContextId を body で返し、Admin access credential TTL は通常の protected request で延長されない。

## 3. Backend HTTP 境界

- [ ] 3.1 `packages/backend/internal/adapter/http/product/auth.go` を更新し、Cookie または Bearer から Product credential source を exactly one として抽出し、`X-Auth-Context-Id` と credential 所属を検証し、曖昧な credential を拒否する。完了条件: middleware が raw token を handler に渡さず、認可済み session context と authContextId を束縛する。
- [ ] 3.2 `packages/backend/internal/adapter/http/product/auth.go` に unsafe な Cookie request の Product Origin validation を追加し、`packages/backend/internal/adapter/http/product/auth_test.go` で検証する。完了条件: 許可されていない Origin または欠落した Origin が protected state mutation 前に拒否される。
- [ ] 3.3 `packages/backend/internal/adapter/http/product/auth.go` に refresh endpoint を除く protected Cookie access-credential state-changing request の Product CSRF validation を追加し、`packages/backend/internal/adapter/http/product/auth_test.go` で検証する。完了条件: handler 実行前に `X-CSRF-Token` が session-bound metadata と `X-Auth-Context-Id` に対して比較され、Cookie refresh は事前 CSRF / selector を要求しない。
- [ ] 3.4 `packages/backend/internal/adapter/http/product/router.go` の `access_token` と `refresh_token` 用 Cookie helper を、Set-Cookie と clear behavior を含めて更新する。完了条件: Product Cookie login/refresh/logout が正しい HttpOnly Cookie を設定・削除し、access Cookie と refresh Cookie はそれぞれ保護 API 用 Path と refresh endpoint 用 Path に限定される。
- [ ] 3.5 `packages/backend/internal/adapter/http/product/router.go` の Product strict handler を更新し、passkey finish、register、refresh、logout、passkey management、account settings、session management で mode 別の generated DTO と authContextId を使う。完了条件: handler が Cookie request のために `Authorization` を直接読まず、Cookie refresh は同送 access Cookie を認可材料にせず、Bearer refresh は `Authorization` header を拒否する。
- [ ] 3.6 `packages/backend/internal/adapter/http/product/router_test.go` に recovery/device-link scenario `[AUTH-BE-S004]`、`[AUTH-BE-S005]`、`[AUTH-BE-S006]`、`[AUTH-BE-S030]`、`[AUTH-BE-S047]`、`[AUTH-BE-S073]` の backend endpoint test を追加または更新する。完了条件: token issuance/consume/device-link Cookie session の coverage がある。
- [ ] 3.7 `packages/backend/internal/adapter/http/product/router_test.go` に recovery register scenario `[AUTH-BE-S007]`、`[AUTH-BE-S008]`、`[AUTH-BE-S048]` の backend endpoint test を追加または更新する。完了条件: register が mode 別 session を返し、recovery/device-link post-processing を維持する。
- [ ] 3.8 `packages/backend/internal/adapter/http/product/router_test.go` と `packages/backend/internal/adapter/http/product/auth_test.go` に passkey management scenario `[AUTH-BE-S014]`、`[AUTH-BE-S015]`、`[AUTH-BE-S016]`、`[AUTH-BE-S017]`、`[AUTH-BE-S018]`、`[AUTH-BE-S019]`、`[AUTH-BE-S074]` の backend endpoint test を追加または更新する。完了条件: Cookie/Bearer session source と ambiguity rejection が網羅される。
- [ ] 3.9 `packages/backend/internal/adapter/http/product/router_test.go` に WebAuthn reauthentication scenario `[AUTH-BE-S028]`、`[AUTH-BE-S029]`、`[AUTH-BE-S036]`、`[AUTH-BE-S037]` の backend endpoint test を追加または更新する。完了条件: high-risk operation が、受け入れ可能な session credential source のどちらでも reauthentication を要求する。
- [ ] 3.10 `packages/backend/internal/adapter/http/product/router_test.go` に device-link scenario `[AUTH-BE-S049]` と `[AUTH-BE-S050]` の backend endpoint test を追加または更新する。完了条件: device-link delivery が有効なアプリケーションセッションと reauth で成功し、reauth なしで失敗する。
- [ ] 3.11 `packages/backend/internal/adapter/http/product/router_test.go`、`packages/backend/internal/adapter/http/product/auth_test.go`、`packages/backend/internal/application/auth_service_test.go` に session issuance/authorization scenario `[AUTH-BE-S001]`、`[AUTH-BE-S002]`、`[AUTH-BE-S003]`、`[AUTH-BE-S009]`、`[AUTH-BE-S010]`、`[AUTH-BE-S054]`、`[AUTH-BE-S055]`、`[AUTH-BE-S058]`、`[AUTH-BE-S060]`、`[AUTH-BE-S063]`、`[AUTH-BE-S075]`、`[AUTH-BE-S076]`、`[AUTH-BE-S077]`、`[AUTH-BE-S078]`、`[AUTH-BE-S079]`、`[AUTH-BE-S080]`、`[AUTH-BE-S081]` の backend endpoint/application test を追加または更新する。完了条件: Bearer mode、Cookie mode、logout、missing/expired/suspended、CSRF、protected access credential ambiguity、refresh credential handling、Cookie refresh の事前 selector / CSRF 非要求、authContextId 所属外拒否、access TTL 非延長が網羅される。
- [ ] 3.12 `packages/backend/internal/adapter/http/product/router_test.go` と `packages/backend/internal/application/auth_service_test.go` に suspended account revocation scenario `[AUTH-BE-S056]`、`[AUTH-BE-S057]`、`[AUTH-BE-S059]` の backend endpoint/application test を追加または更新する。完了条件: suspend による account-wide session revocation timestamp、restore 後の旧 credential 拒否、`account-suspended` failure response shape が Bearer / Cookie / refresh で固定される。
- [ ] 3.13 `packages/backend/internal/application/product_admin_auth_boundary_test.go` と `packages/backend/internal/adapter/http/admin/router_test.go` に Product/Admin auth boundary scenario `[AUTH-BE-S061]`、`[AUTH-BE-S067]`、`[AUTH-BE-S068]`、`[AUTH-BE-S069]`、`[AUTH-BE-S070]`、`[AUTH-BE-S071]`、`[AUTH-BE-S072]` の backend boundary test または import-boundary test を追加または更新する。完了条件: Product account auth と Admin operator auth が単一 switch や相互 import に畳み込まれず、中立 token primitive だけを共有する。
- [ ] 3.14 `packages/backend/internal/adapter/http/admin/auth.go` を更新し、Admin Cookie または Admin Bearer から operator credential source を exactly one として抽出し、`X-Auth-Context-Id` と credential 所属を検証し、Admin Console は Bearer を生成しない境界を保つ。完了条件: Admin middleware が raw token を handler に渡さず、認可済み operator session context と authContextId を束縛する。
- [ ] 3.15 `packages/backend/internal/adapter/http/admin/auth.go` に refresh endpoint を除く protected Admin Cookie access-credential request の Admin Origin validation と Admin CSRF validation を追加・更新し、`packages/backend/internal/adapter/http/admin/router_test.go` で検証する。完了条件: 許可されていない Origin、欠落した Origin、欠落または不一致の `X-Auth-Context-Id` / `X-CSRF-Token` が Admin mutation 前に拒否され、Admin Cookie refresh は事前 CSRF / selector を要求しない。
- [ ] 3.16 `packages/backend/internal/adapter/http/admin/router.go` の `admin_access_token` と `admin_refresh_token` 用 Cookie helper を、Set-Cookie と clear behavior を含めて更新する。完了条件: Admin login/setup/refresh/logout が正しい HttpOnly Cookie を設定・削除し、Admin access Cookie と Admin refresh Cookie はそれぞれ保護 Admin API 用 Path と Admin refresh endpoint 用 Path に限定される。
- [ ] 3.17 `packages/backend/internal/adapter/http/admin/router.go` と `packages/backend/internal/adapter/http/admin/session_validator.go` を更新し、login/setup/operator-setup/refresh/logout/current/passkey management/account mutation で mode 別 session DTO と authContextId を使う。完了条件: handler が Admin Cookie request のために `Authorization` を直接読まず、Admin Cookie refresh は同送 access Cookie を認可材料にせず、Admin Bearer refresh は `Authorization` header を拒否する。

## 4. Frontend Auth State と API Calls

- [ ] 4.1 `packages/frontend/domain/src/auth/types.ts` を更新し、Web auth state が `accessToken` なしで active auth context metadata、authContextId、session metadata、CSRF token を保存するようにする。完了条件: TypeScript type 上、browser-readable な Product accessToken を Web hook から利用できない。
- [ ] 4.2 `packages/frontend/domain/src/auth/session/state.ts` を更新し、Authorization header generation を same-origin credential request helper、`X-Auth-Context-Id` header helper、CSRF header helper に置き換える。完了条件: Product Web domain code に `Authorization: Bearer` 作成 path がない。
- [ ] 4.3 `packages/frontend/domain/src/auth/session/hook.svelte.ts` を更新し、bootstrap refresh、session-expired refresh-once retry、logout、session clearing、account-suspended routing、AccountSetting snapshot handling を扱う。完了条件: auth state が Cookie response、authContextId、CSRF token rotation によって駆動される。
- [ ] 4.4 `packages/frontend/domain/src/auth/passkey/hook.svelte.ts` を更新し、`credentialMode="cookie"` を送信して Cookie session response の authContextId / CSRF / session metadata を受け入れる。完了条件: login が accessToken を decode または保存しない。
- [ ] 4.5 `packages/frontend/domain/src/auth/recovery/hook.svelte.ts` を更新し、register で `credentialMode="cookie"` を送信して authContextId / CSRF / session metadata を受け入れる。完了条件: recovery/device-link registration が token body なしで authenticated state に入る。
- [ ] 4.6 `packages/frontend/domain/src/auth/passkey/management/hook.svelte.ts` と `packages/frontend/domain/src/auth/session/session_api.ts` を更新し、mutation で same-origin credential、`X-Auth-Context-Id`、CSRF header を送信する。完了条件: passkey/device/session management が caller から Authorization header を受け取らない。
- [ ] 4.7 `packages/frontend/domain/src/account/hook.svelte.ts` と `packages/frontend/domain/src/account/localeSync.svelte.ts` を更新し、Cookie + `X-Auth-Context-Id` + CSRF request helper を使う。完了条件: AccountSetting load/update が bearer header なしで動作する。
- [ ] 4.8 `packages/frontend/app/src/tests/mocks/handlers.ts`、`packages/frontend/app/src/tests/mocks/server.ts`、`packages/frontend/app/src/tests/routes/login-page.test.ts`、`packages/frontend/app/src/tests/routes/settings-page.test.ts`、`packages/frontend/app/src/tests/components/DeviceManager.test.ts`、`packages/frontend/app/src/tests/components/PasskeyList.test.ts`、`packages/frontend/app/src/tests/hooks/usePasskeyManagement.test.ts` を更新し、Product Cookie mode response body を返す。完了条件: frontend test が Product Web 用の mock `accessToken` body に依存しない。

## 5. Frontend Tests

- [ ] 5.1 `packages/frontend/domain/src/auth/passkey/hook.svelte.ts`、`packages/frontend/domain/src/auth/session/hook.test.ts`、`packages/frontend/app/src/tests/routes/login-page.test.ts` に login scenario `[AUTH-FE-S001]`、`[AUTH-FE-S002]`、`[AUTH-FE-S051]` の frontend test を追加または更新する。完了条件: passkey login が accessToken を保存せず、CSRF/session metadata から authenticated state に入る。
- [ ] 5.2 `packages/frontend/domain/src/auth/recovery/hook.svelte.ts`、`packages/frontend/domain/src/auth/session/hook.test.ts` に recovery/device-link scenario `[AUTH-FE-S003]`、`[AUTH-FE-S004]`、`[AUTH-FE-S005]`、`[AUTH-FE-S038]`、`[AUTH-FE-S052]` の frontend test を追加または更新する。完了条件: recovery registration が Cookie session response を受け入れる。
- [ ] 5.3 `packages/frontend/domain/src/auth/session/hook.test.ts`、`packages/frontend/domain/src/auth/session/state.test.ts` に refresh/session continuation scenario `[AUTH-FE-S045]`、`[AUTH-FE-S024]`、`[AUTH-FE-S046]`、`[AUTH-FE-S026]`、`[AUTH-FE-S047]` の frontend test を追加または更新する。完了条件: bootstrap refresh、retry-on-session-expired、persistent token storage 不使用、frontend が Cookie header を個別制御せず server refresh outcome に従うことが網羅される。
- [ ] 5.4 `packages/frontend/domain/src/auth/session/hook.test.ts` と `packages/frontend/domain/src/auth/session/state.test.ts` に auth context switching scenario `[AUTH-FE-S048]`、`[AUTH-FE-S028]`、`[AUTH-FE-S049]`、`[AUTH-FE-S050]`、`[AUTH-FE-S031]` の frontend test を追加または更新する。完了条件: browser-readable token-list behavior が Product Web から削除され、server-issued authContextId switching が維持される。
- [ ] 5.5 `packages/frontend/domain/src/auth/session/hook.test.ts` と `packages/frontend/app/src/tests/routes/login-page.test.ts` に expiry/logout routing scenario `[AUTH-FE-S006]`、`[AUTH-FE-S007]`、`[AUTH-FE-S008]` の frontend test を追加または更新する。完了条件: missing session、expired session、logout route intent が区別される。
- [ ] 5.6 `packages/frontend/domain/src/auth/passkey/management/hook.svelte.ts`、`packages/frontend/domain/src/auth/passkey/management/state.test.ts`、`packages/frontend/app/src/tests/components/PasskeyList.test.ts`、`packages/frontend/app/src/tests/hooks/usePasskeyManagement.test.ts` に passkey management scenario `[AUTH-FE-S010]`、`[AUTH-FE-S011]`、`[AUTH-FE-S012]`、`[AUTH-FE-S013]`、`[AUTH-FE-S014]`、`[AUTH-FE-S015]`、`[AUTH-FE-S035]`、`[AUTH-FE-S037]` の frontend test を追加または更新する。完了条件: management API call が期待どおり credential と CSRF を含む。
- [ ] 5.7 `packages/frontend/domain/src/auth/session/hook.test.ts`、`packages/frontend/domain/src/auth/session/state.test.ts`、`packages/frontend/app/src/tests/routes/login-page.test.ts` に security presentation scenario `[AUTH-FE-S019]`、`[AUTH-FE-S020]`、`[AUTH-FE-S054]` の frontend test を追加または更新する。完了条件: token/Cookie value が state/storage に存在せず、auth route が no-store/security behavior を維持する。
- [ ] 5.8 `packages/frontend/domain/src/auth/session/hook.test.ts` と `packages/frontend/domain/src/account/hook.test.ts` に suspended account scenario `[AUTH-FE-S041]`、`[AUTH-FE-S042]`、`[AUTH-FE-S043]`、`[AUTH-FE-S044]` の frontend test を追加または更新する。完了条件: suspended handling が Cookie session state を消去し、public enumeration を避ける。
- [ ] 5.9 `packages/frontend/domain/src/auth/session/session_api.ts`、`packages/frontend/domain/src/auth/session/hook.test.ts`、`packages/frontend/app/src/tests/components/DeviceManager.test.ts` に device management scenario `[AUTH-FE-S034]`、`[AUTH-FE-S053]`、`[AUTH-FE-S036]` の frontend test を追加または更新する。完了条件: session list/revoke/revoke-others が Cookie + `X-Auth-Context-Id` + CSRF request を使う。

## 6. Admin Credential Session と Package Boundary

- [ ] 6.1 `packages/backend/internal/adapter/http/admin/router_test.go`、`packages/backend/internal/application/admin/auth/service_test.go`、`packages/backend/internal/application/admin/auth/authorization_test.go`、`packages/backend/internal/adapter/valkey/auth_state_repository_test.go`、`packages/backend/internal/platform/config/types_test.go` に Admin backend credential session scenario `[ADMIN-AUTH-BE-S056]`、`[ADMIN-AUTH-BE-S057]`、`[ADMIN-AUTH-BE-S058]`、`[ADMIN-AUTH-BE-S059]`、`[ADMIN-AUTH-BE-S060]`、`[ADMIN-AUTH-BE-S061]`、`[ADMIN-AUTH-BE-S062]`、`[ADMIN-AUTH-BE-S063]`、`[ADMIN-AUTH-BE-S064]`、`[ADMIN-AUTH-BE-S065]`、`[ADMIN-AUTH-BE-S067]`、`[ADMIN-AUTH-BE-S068]`、`[ADMIN-AUTH-BE-S069]`、`[ADMIN-AUTH-BE-S070]`、`[ADMIN-AUTH-BE-S071]`、`[ADMIN-AUTH-BE-S072]`、`[ADMIN-AUTH-BE-S073]`、`[ADMIN-AUTH-BE-S074]`、`[ADMIN-AUTH-BE-S075]`、`[ADMIN-AUTH-BE-S076]`、`[ADMIN-AUTH-BE-S077]`、`[ADMIN-AUTH-BE-S078]` の backend/admin test を追加または更新する。完了条件: Admin Cookie credential、Admin Bearer credential、`X-Auth-Context-Id`、CSRF / Origin、Admin Valkey namespace、setup/operator-setup、passkey management、Product host 到達不能性、refresh credential handling、Admin Cookie refresh の事前 selector / CSRF 非要求、operator access TTL 非延長が mode 別に固定される。
- [ ] 6.2 `packages/admin/domain/src/auth.test.ts`、`packages/admin/domain/src/accounts.test.ts`、`packages/admin/api/src/client.test.ts`、`packages/admin/app/src/routes/static-client-cleanup.test.ts` に Admin frontend auth scenario `[ADMIN-AUTH-FE-S027]`、`[ADMIN-AUTH-FE-S028]`、`[ADMIN-AUTH-FE-S029]`、`[ADMIN-AUTH-FE-S030]`、`[ADMIN-AUTH-FE-S031]`、`[ADMIN-AUTH-FE-S033]`、`[ADMIN-AUTH-FE-S034]`、`[ADMIN-AUTH-FE-S035]`、`[ADMIN-AUTH-FE-S036]`、`[ADMIN-AUTH-FE-S037]`、`[ADMIN-AUTH-FE-S038]`、`[ADMIN-AUTH-FE-S039]`、`[ADMIN-AUTH-FE-S040]` の admin frontend/domain test を追加または更新する。完了条件: Admin login/current/refresh/session-expiry/setup/operator-setup が HttpOnly credential Cookie + HttpOnly refresh Cookie + `X-Auth-Context-Id` + CSRF token 契約を使い、operator accessToken と Product auth SDK を browser state / request header に使わず、frontend が Cookie header を個別制御せず Admin backend refresh outcome に従う。
- [ ] 6.3 `packages/admin/api/src/client.ts` を更新する。完了条件: Admin Console wrapper は `credentials: 'same-origin'`、`X-Auth-Context-Id` header helper、CSRF header helper だけを提供し、Bearer Authorization helper や `accessToken` 入力は Admin API / automation client 専用 wrapper へ分離する。
- [ ] 6.4 `packages/admin/domain/src/auth.ts`、`packages/admin/domain/src/accounts.ts`、`packages/admin/domain/src/operators.ts`、`packages/admin/domain/src/hooks/useAdminSession.svelte.ts`、`packages/admin/domain/src/hooks/useAdminLogin.svelte.ts`、`packages/admin/domain/src/hooks/useAdminInitialSetup.svelte.ts`、`packages/admin/domain/src/hooks/useAdminOperatorSetup.svelte.ts`、`packages/admin/domain/src/hooks/useAdminAccounts.svelte.ts`、`packages/admin/domain/src/hooks/useAdminOperators.svelte.ts` を更新する。完了条件: Admin domain state は authContextId、operator/session metadata、CSRF token だけを保持し、operator accessToken、refreshToken、Cookie value、Product SDK、raw fetch に依存しない。
- [ ] 6.5 `packages/admin/app/src/routes/+layout.svelte`、`packages/admin/app/src/routes/login/+page.svelte`、`packages/admin/app/src/routes/setup/+page.svelte`、`packages/admin/app/src/routes/operator-setup/+page.svelte`、`packages/admin/app/src/routes/accounts/+page.svelte`、`packages/admin/app/src/routes/accounts/[id]/+page.svelte`、`packages/admin/app/src/routes/passkeys/+page.svelte`、`packages/admin/app/src/routes/settings/operators/+page.svelte` の import boundary を確認・更新する。完了条件: Admin app は `@www-template/admin-domain` 経由で API を呼び、`@www-template/admin-api`、`@www-template/api`、`@www-template/domain`、raw fetch、package-local BFF route、Authorization 前提 comment を直接使わない。
- [ ] 6.6 `packages/admin/api/src/generated/client.ts`、`packages/admin/api/src/client.ts`、`packages/admin/api/src/index.ts` を確認・更新する。完了条件: Admin SDK は Admin origin `/api/v1/*` operation、Cookie session response、Admin Bearer session response、`X-Auth-Context-Id`、CSRF header を公開し、Product Web DTO、Product auth operation、`/api/admin/*`、Admin Console 向け browser-readable operator accessToken を公開しない。
- [ ] 6.7 `pnpm test:admin` を実行する。完了条件: Admin API SDK、Admin domain、Admin app の Cookie + authContextId session test が repository script 経由で成功する。
- [ ] 6.8 `packages/backend/internal/adapter/http/admin/router_test.go`、`packages/backend/internal/application/admin/auth/authorization_test.go`、`packages/backend/internal/application/admin/account_creation_service_test.go`、`packages/backend/internal/application/product_admin_auth_boundary_test.go` に Admin Console backend boundary scenario `[ADMIN-CONSOLE-BE-S056]`、`[ADMIN-CONSOLE-BE-S057]`、`[ADMIN-CONSOLE-BE-S058]`、`[ADMIN-CONSOLE-BE-S068]`、`[ADMIN-CONSOLE-BE-S069]` の backend/admin test を追加または更新する。完了条件: Admin 管理 API surface と RBAC controller が Product binary / Product bearer / browser-readable operator accessToken を認可材料にせず、Admin credential、`X-Auth-Context-Id`、operator session record、CSRF binding、application authorization use case だけで protected operation を許可する。

## 7. Verification と Artifact Maintenance

- [ ] 7.1 `pnpm lint` を実行する。完了条件: repository script 経由の lint が通る。
- [ ] 7.2 `pnpm check` を実行する。完了条件: TypeScript/Svelte/Go の type check が repository script 経由で通る。
- [ ] 7.3 `pnpm test:server` を実行する。完了条件: 2.5、3.6、3.7、3.8、3.9、3.10、3.11、3.12、3.13、6.1、6.8 で明示列挙した backend scenario の test が通る。
- [ ] 7.4 `pnpm test:client` を実行する。完了条件: 5.1、5.2、5.3、5.4、5.5、5.6、5.7、5.8、5.9 で明示列挙した Product frontend scenario の test が通る。
- [ ] 7.5 `pnpm test:admin` を実行する。完了条件: 6.2、6.3、6.4、6.5、6.6 で明示した Admin API/domain/app Cookie + authContextId session test が通る。
- [ ] 7.6 すべての implementation edit 後に `pnpm check:codegen` を実行する。完了条件: Product/Admin TypeSpec generated artifact が同期し、surface contamination がない。
- [ ] 7.7 実装中に contract mismatch が見つかった場合、OpenSpec delta specs/design/tasks を更新する。完了条件: archive または sync 前に artifact と code が一致している。
- [ ] 7.8 `pnpm test:e2e` を実行する。完了条件: Product Web と Admin Console の login / current / refresh / protected mutation / logout browser behavior が Cookie + authContextId browser surface と Product/Admin external Bearer 分離を満たす。
